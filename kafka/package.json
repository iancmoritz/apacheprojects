{
  "name": "kafka-browser",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A real Apache Kafka KRaft broker running entirely in the browser on CheerpJ",
  "scripts": {
    "assets": "node scripts/fetch-jars.mjs && node scripts/build-glue.mjs",
    "dev": "npm run assets && vite",
    "build": "npm run assets && tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "5.9.3",
    "vite": "8.1.5"
  }
}
